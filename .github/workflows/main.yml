name: Build APK CASOS

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          accept-licenses: true

      - name: Accept SDK licenses manually
        run: |
          yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses || true
          yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses --no_https || true
          export JAVA_OPTS="-Djava.awt.headless=true"
          yes | $ANDROID_HOME/tools/bin/sdkmanager --licenses || true

      - name: Install SDK components
        run: |
          yes | $ANDROID_HOME/tools/bin/sdkmanager "platform-tools"
          yes | $ANDROID_HOME/tools/bin/sdkmanager "platforms;android-33"
          yes | $ANDROID_HOME/tools/bin/sdkmanager "build-tools;33.0.0"

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Clean project
        run: ./gradlew clean

      - name: Build APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: CASOS-APK
          path: app/build/outputs/apk/debug/app-debug.apk
